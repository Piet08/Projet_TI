import { RatingMapPipe } from './rating-map.pipe';

describe('RatingMapPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingMapPipe();
    expect(pipe).toBeTruthy();
  });
});
